import 'package:flutter/material.dart';

class SpendHomePage extends StatelessWidget {
  const SpendHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Spend Home Page'),
      ),
    );
  }
}
