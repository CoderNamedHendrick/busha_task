import 'package:flutter/material.dart';

class EarnHomePage extends StatelessWidget {
  const EarnHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Earn Home Page'),
      ),
    );
  }
}
