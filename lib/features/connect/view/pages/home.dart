import 'package:flutter/material.dart';

class ConnectHomePage extends StatelessWidget {
  const ConnectHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Connect Home Page'),
      ),
    );
  }
}
